export class School {
  constructor(public id?: string,
              public name?: string,
              public websiteUrl?: string,
              public avgRating?: number,
              public address?: string) {
  }
}
